"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// tests/parcel_selection_normalization.test.ts
var import_strict = __toESM(require("node:assert/strict"), 1);
var import_node_test = __toESM(require("node:test"), 1);

// src/frontend/utils/safe.ts
function safeString(value, fallback = "") {
  try {
    if (typeof value === "number" || typeof value === "boolean" || typeof value === "bigint") {
      return String(value);
    }
    if (typeof value !== "string") {
      return fallback;
    }
    const trimmed = value.trim();
    return trimmed.length > 0 ? trimmed : fallback;
  } catch {
    return fallback;
  }
}

// src/frontend/utils/earth_grid_coordinates.ts
function centered(value, width) {
  return ((value + width / 2) % width + width) % width - width / 2;
}
function normalizeLongitude(value) {
  return centered(value, 360);
}
function earthGridWorldPointToLonLat(worldX, worldZ, frame) {
  const gridX = frame.storageOrigin.x + worldX;
  const gridZ = frame.storageOrigin.z + worldZ;
  return [
    normalizeLongitude(
      frame.centralMeridianDegrees + gridX / frame.worldWidthCells * 360
    ),
    gridZ / frame.worldHeightCells * 180
  ];
}
function earthGridLonLatToWorld(longitude, latitude, frame) {
  if (!Number.isFinite(longitude) || !Number.isFinite(latitude)) return null;
  const longitudeDelta = centered(
    normalizeLongitude(longitude) - frame.centralMeridianDegrees,
    360
  );
  const gridX = longitudeDelta / 360 * frame.worldWidthCells;
  const gridZ = latitude / 180 * frame.worldHeightCells;
  return [
    centered(gridX - frame.storageOrigin.x, frame.worldWidthCells),
    gridZ - frame.storageOrigin.z
  ];
}

// src/frontend/world_edit/systems/parcel/system.ts
function parcelSelectionActionForIntent(action) {
  if (action === "primary") return "select";
  if (action === "secondary") return "remove";
  return null;
}

// src/frontend/world_edit/systems/stair/quick_settings.ts
var DEFAULT_STAIR_TOOL_PARAMETERS = Object.freeze({
  stairType: "straight",
  widthMm: 1e3,
  treadCount: 15,
  startSide: "bottom",
  endSide: "top",
  direction: "up"
});

// src/frontend/world_edit/systems/roof/contracts.ts
var DEFAULT_ROOF_TOOL_PARAMETERS = Object.freeze({
  roofType: "gable",
  pitchDeg: 35,
  eavesHeightMm: 6e3,
  ridgeDirection: "auto",
  overhangMm: 500,
  overhangNorthMm: 500,
  overhangEastMm: 500,
  overhangSouthMm: 500,
  overhangWestMm: 500,
  edgeOverhangsMm: [],
  roofSkinThicknessMm: 200,
  roofSkinMaterial: "clay-roof-tile",
  insulationMode: "between",
  insulationThicknessMm: 200,
  sheathingThicknessMm: 22,
  underlayThicknessMm: 3,
  counterBattenWidthMm: 60,
  counterBattenHeightMm: 40,
  tileBattenWidthMm: 50,
  tileBattenHeightMm: 30,
  tileBattenSpacingMm: 330,
  roofTileThicknessMm: 20,
  roofTileMaterial: "clay-roof-tile",
  rafterWidthMm: 80,
  rafterHeightMm: 200,
  rafterSpacingMm: 650,
  birdsmouthDepthMm: 30,
  purlinWidthMm: 140,
  purlinHeightMm: 200,
  purlinMaximumSpacingMm: 4500,
  purlinMiddleSpanThresholdMm: 4500,
  plateauWidthRatio: 0.25,
  mansardBreakRatio: 0.38,
  mansardLowerPitchDeg: 70,
  mansardUpperPitchDeg: 28,
  hipEndRatio: 0.5,
  barrelRiseMm: 3e3,
  barrelSegmentCount: 12,
  sawtoothCount: 3,
  sawtoothPitchDeg: 35
});

// src/frontend/world_edit/systems/roof/quick_settings.ts
var ROOF_TYPE_OPTIONS = Object.freeze([
  { type: "flat", label: "Flachdach", icon: '<path d="M3 16h18M5 12h14v4H5z"/>' },
  { type: "gable", label: "Satteldach", icon: '<path d="m3 16 9-9 9 9M5 16h14"/>' },
  { type: "hipped", label: "Walmdach", icon: '<path d="m3 16 5-8h8l5 8M3 16h18M8 8l4 8 4-8"/>' },
  { type: "half_hipped", label: "Kr\xFCppelwalm", icon: '<path d="m3 16 5-7h8l5 7M3 16h18M8 9l2 3h4l2-3"/>' },
  { type: "pent", label: "Pultdach", icon: '<path d="M4 16 20 8v8H4z"/>' },
  { type: "mansard", label: "Mansarddach", icon: '<path d="m3 16 4-6 3-3h4l3 3 4 6M3 16h18"/>' },
  { type: "trapezoid", label: "Trapezdach", icon: '<path d="m4 16 4-8h8l4 8H4z"/>' },
  { type: "butterfly", label: "Schmetterling", icon: '<path d="m3 8 9 8 9-8M5 16h14"/>' },
  { type: "pyramid", label: "Zeltdach", icon: '<path d="m3 16 9-9 9 9H3zM12 7v9"/>' },
  { type: "barrel", label: "Tonnendach", icon: '<path d="M4 16C4 5 20 5 20 16M4 16h16"/>' },
  { type: "sawtooth", label: "Sheddach", icon: '<path d="M3 16V9l5 4V7l5 6V7l8 9H3z"/>' }
]);

// src/frontend/world_edit/world_edit_controller.ts
function asRecord(value) {
  return value && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function asArray(value) {
  return Array.isArray(value) ? value : [];
}
function normalizedParcelItems(value, maximum) {
  const byId = /* @__PURE__ */ new Map();
  for (const item of asArray(value)) {
    const record = asRecord(item);
    const parcelId = safeString(
      record.parcelId ?? record.parcel_id ?? record.featureId ?? record.id,
      ""
    );
    const geometry = asRecord(record.geometry);
    const geometryType = safeString(geometry.type, "");
    const rawPolygons = geometryType === "Polygon" ? [asArray(geometry.coordinates)] : geometryType === "MultiPolygon" ? [...asArray(geometry.coordinates)] : [];
    if (!parcelId || rawPolygons.length === 0) continue;
    const polygons = [];
    for (const polygon2 of rawPolygons) {
      const rings = asArray(polygon2);
      const exterior = asArray(rings[0]);
      if (exterior.length < 3) continue;
      polygons.push(rings);
    }
    if (polygons.length === 0) continue;
    const existing = byId.get(parcelId);
    byId.set(parcelId, {
      parcelId,
      datasetId: safeString(record.datasetId ?? record.dataset_id, existing?.datasetId ?? "flurstuecke"),
      geometry: geometryType === "Polygon" ? { type: "Polygon", coordinates: polygons[0] } : { type: "MultiPolygon", coordinates: polygons },
      properties: { ...asRecord(existing?.properties), ...asRecord(record.properties) }
    });
  }
  return [...byId.values()].slice(0, Math.max(1, maximum));
}

// tests/parcel_selection_normalization.test.ts
function polygon(maximum) {
  return {
    type: "Polygon",
    coordinates: [[
      [0, 0],
      [maximum, 0],
      [maximum, maximum],
      [0, maximum],
      [0, 0]
    ]]
  };
}
(0, import_node_test.default)("a newer parcel snapshot replaces an older geometry instead of being appended", () => {
  const normalized = normalizedParcelItems([
    { parcelId: "parcel-1", datasetId: "flurstuecke", geometry: polygon(1) },
    { parcelId: "parcel-1", datasetId: "flurstuecke", geometry: polygon(4) }
  ], 64);
  import_strict.default.equal(normalized.length, 1);
  import_strict.default.equal(normalized[0]?.geometry.type, "Polygon");
  import_strict.default.deepEqual(normalized[0]?.geometry, polygon(4));
});
(0, import_node_test.default)("left click selects and right click removes a parcel", () => {
  import_strict.default.equal(parcelSelectionActionForIntent("primary"), "select");
  import_strict.default.equal(parcelSelectionActionForIntent("secondary"), "remove");
  import_strict.default.equal(parcelSelectionActionForIntent("primary-release"), null);
  import_strict.default.equal(parcelSelectionActionForIntent("secondary-release"), null);
});
(0, import_node_test.default)("parcel coordinates use the same canonical Earth grid as visible chunk overlays", () => {
  const frame = {
    worldWidthCells: 4e7,
    worldHeightCells: 2e7,
    centralMeridianDegrees: 0,
    storageOrigin: { x: 1489440, z: 5835552 }
  };
  const berlin = [13.405, 52.52];
  const east = earthGridLonLatToWorld(berlin[0] + 1e-3, berlin[1], frame);
  const world = earthGridLonLatToWorld(berlin[0], berlin[1], frame);
  import_strict.default.ok(world);
  import_strict.default.ok(east);
  import_strict.default.ok(Math.abs(east[0] - world[0] - 1e-3 / 360 * frame.worldWidthCells) < 1e-8);
  const roundTrip = earthGridWorldPointToLonLat(world[0], world[1], frame);
  import_strict.default.ok(Math.abs(roundTrip[0] - berlin[0]) < 1e-10);
  import_strict.default.ok(Math.abs(roundTrip[1] - berlin[1]) < 1e-10);
});
(0, import_node_test.default)("a real MultiPolygon keeps all of its parts", () => {
  const first = polygon(1).coordinates;
  const second = {
    type: "Polygon",
    coordinates: [[[3, 3], [4, 3], [4, 4], [3, 4], [3, 3]]]
  }.coordinates;
  const normalized = normalizedParcelItems([{
    parcelId: "parcel-2",
    datasetId: "flurstuecke",
    geometry: { type: "MultiPolygon", coordinates: [first, second] }
  }], 64);
  import_strict.default.equal(normalized.length, 1);
  import_strict.default.equal(normalized[0]?.geometry.type, "MultiPolygon");
  import_strict.default.equal((normalized[0]?.geometry.coordinates).length, 2);
});
